name: CI - Testes e Qualidade

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  PHP_VERSION: '8.2'

jobs:
  test:
    name: 🧪 Testes e Qualidade de Código
    runs-on: ubuntu-latest
    
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: rootpassword
          MYSQL_DATABASE: aula_php_mvc_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🐘 Configurar PHP ${{ env.PHP_VERSION }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: pdo, pdo_mysql, mbstring, xml, curl
          coverage: xdebug
          tools: composer:v2

      - name: 📋 Verificar versão do PHP
        run: |
          php -v
          composer --version

      - name: 🔍 Validar composer.json
        run: composer validate --strict

      - name: 📦 Cache de dependências do Composer
        uses: actions/cache@v3
        with:
          path: vendor
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-

      - name: 📥 Instalar dependências do Composer
        run: composer install --prefer-dist --no-interaction --no-progress

      - name: 🗄️ Configurar banco de dados de teste
        run: |
          mysql -h 127.0.0.1 -u root -prootpassword aula_php_mvc_test < database/schema.sql || true
        continue-on-error: true

      - name: 🧪 Executar testes unitários
        run: composer test
        env:
          DB_HOST: 127.0.0.1
          DB_NAME: aula_php_mvc_test
          DB_USER: root
          DB_PASS: rootpassword

      - name: 📊 Gerar relatório de cobertura
        run: composer test-coverage
        continue-on-error: true

      - name: 📈 Upload cobertura de código
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
        continue-on-error: true

      - name: 🔍 Análise estática com PHPStan
        run: composer phpstan

      - name: ✨ Verificar padrões de código (PHPCS)
        run: composer cs-check

      - name: 🔒 Verificar sintaxe PHP
        run: find app -name "*.php" -exec php -l {} \;

  security:
    name: 🔒 Análise de Segurança
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🐘 Configurar PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: pdo, pdo_mysql, mbstring
          tools: composer:v2

      - name: 📦 Instalar dependências
        run: composer install --prefer-dist --no-interaction --no-progress

      - name: 🛡️ Verificar dependências vulneráveis
        run: composer audit

      - name: 🔍 Verificar arquivos sensíveis
        run: |
          if [ -f ".env" ]; then
            echo "⚠️ AVISO: Arquivo .env encontrado no repositório!"
            exit 1
          fi
          echo "✅ Nenhum arquivo sensível detectado"

  docker-build:
    name: 🐳 Build Docker Image
    runs-on: ubuntu-latest
    needs: [test, security]
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🐳 Configurar Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔨 Build Docker image
        run: docker build -t aula7-mvc:latest .

      - name: ✅ Verificar imagem Docker
        run: docker images aula7-mvc:latest

  summary:
    name: ✅ Resumo do Pipeline
    runs-on: ubuntu-latest
    needs: [test, security, docker-build]
    if: always()
    
    steps:
      - name: 📊 Status do Pipeline
        run: |
          echo "## 📊 Resumo do Pipeline CI" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Testes:** ${{ needs.test.result }}" >> $GITHUB_STEP_SUMMARY
          echo "� **Segurança:** ${{ needs.security.result }}" >> $GITHUB_STEP_SUMMARY
          echo "🐳 **Docker:** ${{ needs.docker-build.result }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🚀 **Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "👤 **Autor:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
